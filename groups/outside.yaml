# -----------------------------------------
#  Outside
# -----------------------------------------
outside:
  view: yes
  name: Outside
  entities:
    - group.outside_lights
    - group.led_strip_1
    - camera.front_door
    - group.weather
    - group.garage_camera

outside_lights:
  name: Lighting
  entities:
    - light.exterior_porch
    - light.deck
    - light.deck_accent

led_strip_1:
  name: LED Strip 1
  entities:
    - light.led_strip_1
    - input_slider.led_strip_1_speed
